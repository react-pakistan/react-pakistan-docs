"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
describe('Testing ResponsiveScreens', () => {
    it('checks mobileSMin', () => {
        const result = __1.responsiveScreens.mobileSMin;
        expect(result).toEqual({
            name: 'MobileSMin',
            styles: {
                height: '1024px',
                width: '320px',
            },
            type: 'mobile',
        });
    });
    it('checks mobileSMax', () => {
        const result = __1.responsiveScreens.mobileSMax;
        expect(result).toEqual({
            name: 'MobileSMax',
            styles: {
                height: '1024px',
                width: '374px',
            },
            type: 'mobile',
        });
    });
    it('checks mobileMMax', () => {
        const result = __1.responsiveScreens.mobileMMax;
        expect(result).toEqual({
            name: 'MobileMMax',
            styles: {
                height: '1024px',
                width: '424px',
            },
            type: 'mobile',
        });
    });
    it('checks mobileLMin', () => {
        const result = __1.responsiveScreens.mobileLMin;
        expect(result).toEqual({
            name: 'MobileLMin',
            styles: {
                height: '1024px',
                width: '425px',
            },
            type: 'mobile',
        });
    });
    it('checks mobileLMax', () => {
        const result = __1.responsiveScreens.mobileLMax;
        expect(result).toEqual({
            name: 'MobileLMax',
            styles: {
                height: '1024px',
                width: '767px',
            },
            type: 'mobile',
        });
    });
    it('checks tabletMin', () => {
        const result = __1.responsiveScreens.tabletMin;
        expect(result).toEqual({
            name: 'TabletMin',
            styles: {
                height: '100%',
                width: '768px',
            },
            type: 'mobile',
        });
    });
    it('checks tabletMax', () => {
        const result = __1.responsiveScreens.tabletMax;
        expect(result).toEqual({
            name: 'TabletMax',
            styles: {
                height: '100%',
                width: '1023px',
            },
            type: 'mobile',
        });
    });
    it('checks laptopMin', () => {
        const result = __1.responsiveScreens.laptopMin;
        expect(result).toEqual({
            name: 'LaptopMin',
            styles: {
                height: '100%',
                width: '1024',
            },
            type: 'mobile',
        });
    });
    it('checks laptopMax', () => {
        const result = __1.responsiveScreens.laptopMax;
        expect(result).toEqual({
            name: 'LaptopMax',
            styles: {
                height: '100%',
                width: '1439px',
            },
            type: 'mobile',
        });
    });
    it('checks laptopLMin', () => {
        const result = __1.responsiveScreens.laptopLMin;
        expect(result).toEqual({
            name: 'LaptopLMin',
            styles: {
                height: '100%',
                width: '1440px',
            },
            type: 'mobile',
        });
    });
    it('checks laptopLMax', () => {
        const result = __1.responsiveScreens.laptopLMax;
        expect(result).toEqual({
            name: 'LaptopLMax',
            styles: {
                height: '100%',
                width: '2559px',
            },
            type: 'mobile',
        });
    });
    it('checks desktop', () => {
        const result = __1.responsiveScreens.desktop;
        expect(result).toEqual({
            name: 'Desktop',
            styles: {
                height: '100%',
                width: '2560px',
            },
            type: 'mobile',
        });
    });
});
