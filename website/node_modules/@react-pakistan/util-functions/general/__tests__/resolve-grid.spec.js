"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
describe('Testing ResolveGrid function', () => {
    it('checks ResolveGrid 2 length working', () => {
        const result = __1.resolveGrid([1, 2], 'auto');
        expect(result).toBe('repeat(2, auto)');
    });
    it('checks ResolveGrid 5 length working', () => {
        const result = __1.resolveGrid([1, 2, 3, 4, 5], 'auto');
        expect(result).toBe('repeat(5, auto)');
    });
    it('checks ResolveGrid 3 length & 1fr gridContent working', () => {
        const result = __1.resolveGrid([1, 2, 3], '1fr');
        expect(result).toBe('repeat(3, 1fr)');
    });
});
