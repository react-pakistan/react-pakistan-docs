"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
const currentDate = new Date().toLocaleString('en-GB', {
    weekday: 'long',
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
});
describe('Testing ResolveCurrentDate function', () => {
    it('checks resolveCurrentDate working', () => {
        const result = __1.resolveCurrentDate();
        expect(result).toBe(currentDate);
    });
});
