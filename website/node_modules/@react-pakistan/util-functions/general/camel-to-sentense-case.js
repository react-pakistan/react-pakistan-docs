"use strict";
/**
 * A util function to convert camel case into sentense case
 *
 * @param str - string provided to convert camel case into sentense case - taimoorKhan
 * @returns string in sentense case - Taimoo Khan
 * @author Taimoor Khan
 * @remarks
 */
/** End file docs */
Object.defineProperty(exports, "__esModule", { value: true });
exports.camelToSentenseCase = void 0;
exports.camelToSentenseCase = (str) => {
    const newString = str.replace(/([A-Z])/g, ' $1');
    return newString
        .split(' ')
        .map((_str) => _str.charAt(0).toUpperCase() + _str.slice(1))
        .join(' ');
};
