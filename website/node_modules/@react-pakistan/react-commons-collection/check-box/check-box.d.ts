import { ReactElement } from 'react';
import { ICheckBoxProps } from './type';
export declare const CheckBox: ({ check, checkedColor, isChecked, onChange, unCheckedColor, ...rest }: ICheckBoxProps) => ReactElement;
export declare const CheckBoxWithFieldLabel: any;
